# NotmyTelegramCryptobot

Telegram bot that gets BTC and ETH prices

You need to put your bot token in auth_data.py like this

`token = *your token*`

run:

`pip instal -r requirements.txt`

and then:

`python main.py`
